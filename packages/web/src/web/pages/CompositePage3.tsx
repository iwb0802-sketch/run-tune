/*
 * CompositePage3.tsx
 *
 * 복합탭3은 복합탭2와 같은 복합 엔진·세션·측정 흐름을 공유한다.
 * 실제 표기와 경로 상태는 CompositePage에서 구분한다.
 */

import CompositePage from "./CompositePage";

export default function CompositePage3() {
  return <CompositePage />;
}
