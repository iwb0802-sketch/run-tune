/*
 * CompositePage2.tsx
 *
 * 복합탭2는 기존 복합 조율 탭과 같은 검증·세션·측정 로직을 공유한다.
 * 화면과 동작을 의도적으로 동일하게 유지해 두 탭에서 같은 복합 엔진을 사용할 수 있다.
 */

import CompositePage from "./CompositePage";

export default function CompositePage2() {
  return <CompositePage />;
}
